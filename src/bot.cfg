[Locations]
looks_config = ./items.cfg
python_file = ./agent.py
name = Agent
maximum_tick_rate_preference = 240

[Details]
developer = harrix432
description = Essentially, a TAS for Rocket League.
fun_fact = Works best with 1 car, but supports multiple cars to a lesser extent.
github = https://github.com/hb432/skrrripted
language = python
