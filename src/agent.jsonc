{
   // can be one of [view, relay], does nothing at the moment
   "mode": "view",

   // target JSON must contain ball physics data and a "cars" array
   // refer to "./scripts/example.jsonc" for a detailed explanation of things
   "script": "./scripts/showcase.jsonc"
}

/*
   a disclaimer about this little "discount" JSONC file format
   comments, multi-line or otherwise, MUST start at the beginning of a line
   and end at the end of a line
   commented lines must not contain any JSON code whatsoever
*/